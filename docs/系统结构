ADMIN 家族（2个）

ADMIN


ADMIN_REVIEWER

AI_BUILDER 家族（1个）

AI_BUILDER

BUSINESS_OWNER 家族（2个）

BUSINESS_OWNER


BUSINESS_OWNER_APPROVER

DATA_ADMIN 家族（2个）

DATA_ADMIN


DATA_ADMIN_MAINTAINER

EMPLOYEE 家族（2个）

EMPLOYEE


EMPLOYEE_REQUESTER

EXECUTIVE 家族（2个）

EXECUTIVE


EXECUTIVE_COMPLIANCE

SECOPS 家族（2个）

SECOPS


SECOPS_RESPONDER

独立角色（2个，如有）

AUDIT


SEC




一、影子AI发现（客户端检测员工电脑上的未授权AI工具）

ADMIN：可见全公司所有影子AI上报记录，可查看详情、统计数据，可执行队列管理等操作。


ADMIN_REVIEWER：可见全公司所有影子AI上报记录，可查看详情和统计，但无操作权限（不能管理队列、不能处置）。


SECOPS：可见全公司所有影子AI上报记录，可查看详情、统计，并可执行误报标注、关联安全事件等处置操作。


SECOPS_RESPONDER：可见全公司所有影子AI上报记录，可查看详情，可执行基础的误报标注，但无队列管理等高级操作。


EXECUTIVE：可见全公司影子AI的统计摘要（如趋势图、高风险终端数量），不展示具体终端明细。


EXECUTIVE_COMPLIANCE：同样可见全公司统计摘要，侧重合规视角，不展示明细。


DATA_ADMIN：可见全公司影子AI记录，用于数据溯源（例如哪些影子AI访问了敏感数据），可查看明细，无处置权限。


DATA_ADMIN_MAINTAINER：可见全公司影子AI记录，但仅为只读，用于了解数据资产被哪些影子AI访问。


BUSINESS_OWNER：可见本部门范围内的影子AI记录（若系统支持部门字段），否则可见全公司，可查看明细，无处置权限。


BUSINESS_OWNER_APPROVER：同上，可见本部门或全公司的影子AI记录，只读。


AI_BUILDER：可见全公司影子AI记录（用于分析AI工具使用趋势），可查看明细，无处置权限。


EMPLOYEE：仅可见自己名下（os_username 匹配）的影子AI上报记录，可查看明细，无处置权限。


EMPLOYEE_REQUESTER：同样仅可见本人的影子AI记录，可查看明细，无处置权限。


二、员工AI行为监控（监控员工使用授权AI工具的行为统计，如调用次数、违规操作等）

ADMIN：可见全公司员工AI行为监控报表与明细，可配置监控策略。


ADMIN_REVIEWER：可见全公司员工AI行为监控报表与明细，只读，无配置权限。


SECOPS：可见全公司员工AI行为监控报表与明细，可用于安全事件关联分析，可导出数据。


SECOPS_RESPONDER：可见全公司员工AI行为监控报表与明细，只读，可导出。


EXECUTIVE：可见全公司员工AI行为监控的统计摘要（如调用趋势、高风险部门），无明细。


EXECUTIVE_COMPLIANCE：可见全公司员工AI行为监控的合规报表，无明细。


DATA_ADMIN：可见全公司员工AI行为监控，重点关注涉及数据资产的操作行为（如数据导出、敏感数据访问），可查看明细。


DATA_ADMIN_MAINTAINER：可见全公司员工AI行为监控，只读，用于了解数据资产使用情况。


BUSINESS_OWNER：可见本部门员工AI行为监控的统计摘要，可查看明细（用于业务合规）。


BUSINESS_OWNER_APPROVER：同上，可见本部门员工AI行为监控，只读。


AI_BUILDER：可见全公司员工AI行为监控，重点关注自己开发的AI模型的调用情况、成功率、错误率等，可查看明细。


EMPLOYEE：不可见此模块（菜单不显示，接口拒绝访问）。


EMPLOYEE_REQUESTER：不可见此模块。


三、实时威胁监控（检测恶意AI模拟攻击、代理窃取行为，支持告警、阻拦/忽略、规则配置）

ADMIN：可见实时威胁监控面板，可查看告警列表和详情，可配置敏感文件检测规则，但无阻拦/忽略操作（或可配置但建议不操作，由安全运维执行）。


ADMIN_REVIEWER：不可见此模块。


SECOPS：可见完整实时威胁监控功能，包括实时告警、手动阻拦/忽略、配置敏感文件检测规则，可查看详情。


SECOPS_RESPONDER：可见实时告警，可执行阻拦/忽略操作，但无权配置规则（规则配置由 SECOPS 总控负责）。


EXECUTIVE：不可见此模块（如需了解威胁态势，可通过安全指挥台看摘要报表，不进入实时监控页面）。


EXECUTIVE_COMPLIANCE：不可见此模块。


DATA_ADMIN：不可见此模块。


DATA_ADMIN_MAINTAINER：不可见此模块。


BUSINESS_OWNER：不可见此模块。


BUSINESS_OWNER_APPROVER：不可见此模块。


AI_BUILDER：不可见此模块。


EMPLOYEE：不可见此模块。


EMPLOYEE_REQUESTER：不可见此模块。

一、数据资产

ADMIN：完全管理（上传、登记、编辑、删除、查看、下载、审批流转）。可见全公司资产。


ADMIN_REVIEWER：只读（查看列表、详情），无修改操作。


SECOPS：只读，用于安全溯源。


SECOPS_RESPONDER：只读。


EXECUTIVE：不可见（或通过报表看统计，不直接看资产）。


EXECUTIVE_COMPLIANCE：不可见。


DATA_ADMIN：完全管理（上传、编辑、删除、查看）。可见全公司资产。


DATA_ADMIN_MAINTAINER：仅上传和维护（新增、编辑自己负责的资产），无权删除。


BUSINESS_OWNER：只读（本部门或全公司资产，用于业务审计）。


BUSINESS_OWNER_APPROVER：只读。


AI_BUILDER：只读（仅查看与AI模型相关的数据资产）。


EMPLOYEE：不可见。


EMPLOYEE_REQUESTER：不可见。


二、审批管理（审批中心）
包含：审批申请列表、待办、已办、发起申请、通过/拒绝。

ADMIN：可查看所有审批单（全公司），可执行通过/拒绝，可配置审批流程（approval:config）。


ADMIN_REVIEWER：可查看所有审批单，可执行通过/拒绝（用于复核），无配置权限。


SECOPS：可查看与安全相关的审批单（如策略变更、规则调整），可执行通过/拒绝。


SECOPS_RESPONDER：仅查看安全类审批单，无操作权限。


EXECUTIVE：可查看所有审批单（只读报表），无操作。


EXECUTIVE_COMPLIANCE：只查看合规类审批单。


DATA_ADMIN：可查看数据资产相关的审批单（如数据导出申请），可执行通过/拒绝。


DATA_ADMIN_MAINTAINER：仅查看与自己提交或相关的审批单，无操作。


BUSINESS_OWNER：可查看业务类审批单（本部门），可执行通过/拒绝。


BUSINESS_OWNER_APPROVER：同上，可执行通过/拒绝。


AI_BUILDER：仅查看与自己相关的审批单（如AI模型发布申请），无操作。


EMPLOYEE：可查看本人提交的申请单，不可审批。


EMPLOYEE_REQUESTER：可发起申请，可查看本人提交的申请单，不可审批。


三、治理变更（治理变更复核、创建）

ADMIN：可创建、复核、查看所有治理变更。


ADMIN_REVIEWER：可复核、查看，不可创建。


SECOPS：可创建、复核、查看。


SECOPS_RESPONDER：可查看，不可创建、复核。


EXECUTIVE：可查看摘要，不可操作。


EXECUTIVE_COMPLIANCE：可查看，不可操作。


DATA_ADMIN：可创建与数据治理相关的变更，可查看。


DATA_ADMIN_MAINTAINER：不可见。


BUSINESS_OWNER：可创建业务相关的治理变更，可查看。


BUSINESS_OWNER_APPROVER：不可创建，可查看。


AI_BUILDER：不可见。


EMPLOYEE：不可见。


EMPLOYEE_REQUESTER：不可见。


四、策略管理

ADMIN：完全管理（创建、编辑、删除、启停策略）。


ADMIN_REVIEWER：只读，可查看策略列表和详情。


SECOPS：可启停策略（policy:status:toggle），可查看，不可编辑删除。


SECOPS_RESPONDER：只读。


EXECUTIVE：只读。


EXECUTIVE_COMPLIANCE：只读。


DATA_ADMIN：只读（与数据相关的策略）。


DATA_ADMIN_MAINTAINER：只读。


BUSINESS_OWNER：只读。


BUSINESS_OWNER_APPROVER：只读。


AI_BUILDER：只读。


EMPLOYEE：不可见。


EMPLOYEE_REQUESTER：不可见。


五、用户管理

ADMIN：完全管理（创建、编辑、删除、启用禁用、重置密码、分配角色）。


ADMIN_REVIEWER：只读（查看用户列表），不可操作。


SECOPS：不可见（或仅查看与安全事件相关的用户，不可管理）。


其他所有角色：不可见。


六、角色管理

ADMIN：完全管理（创建、编辑、删除角色，分配权限）。


ADMIN_REVIEWER：只读（查看角色列表和权限）。


其他所有角色：不可见。


七、权限管理

ADMIN：完全管理（定义权限码，分配权限给角色）。


ADMIN_REVIEWER：只读（查看权限矩阵）。


其他所有角色：不可见。


八、审计日志与审计报告

ADMIN：可查看全量审计日志，可导出，可生成报告。无删除权限。


ADMIN_REVIEWER：可查看全量审计日志，可生成报告，无导出。


SECOPS：可查看全量审计日志，可导出（用于安全取证），可生成报告。


SECOPS_RESPONDER：可查看（可能只限安全事件相关日志），无导出。


EXECUTIVE：可查看审计报告（聚合统计），不可查看原始日志。


EXECUTIVE_COMPLIANCE：可查看审计报告，不可查看原始日志。


DATA_ADMIN：可查看与数据资产操作相关的审计日志（自动过滤）。


DATA_ADMIN_MAINTAINER：可查看本人操作的审计日志。


BUSINESS_OWNER：可查看与业务申请相关的审计日志。


BUSINESS_OWNER_APPROVER：同上。


AI_BUILDER：不可见。


EMPLOYEE：可查看本人操作的审计日志（如登录、提交申请）。


EMPLOYEE_REQUESTER：同上。


AUDIT（如有）：可查看全量日志、导出、生成报告。


九、SoD规则管理（职责分离规则）

ADMIN：完全管理（创建、编辑、删除、启用/禁用规则）。


ADMIN_REVIEWER：只读（查看规则）。


SECOPS：可查看，可启用/禁用（用于安全合规），不可编辑删除。


其他角色：不可见（除非合规专项可只读）。


十、敏感扫描

ADMIN：可配置扫描策略、启动扫描、查看结果。


ADMIN_REVIEWER：可查看扫描结果，不可配置。


SECOPS：可启动扫描、查看结果、处置敏感发现。


SECOPS_RESPONDER：可查看结果，可处置。


DATA_ADMIN：可查看与数据资产相关的敏感扫描结果，可发起针对自己资产的扫描。


DATA_ADMIN_MAINTAINER：可查看自己资产的扫描结果。


其他角色：不可见。


十一、运维观测

ADMIN：可见全系统运维指标（性能、调用量、错误率等）。


ADMIN_REVIEWER：可见。


SECOPS：可见（用于安全运维）。


SECOPS_RESPONDER：可见。


EXECUTIVE：可见高层运维摘要。


EXECUTIVE_COMPLIANCE：可见合规相关运维指标。


DATA_ADMIN：可见数据资产相关运维指标。


BUSINESS_OWNER：可见业务相关运维指标。


AI_BUILDER：可见AI模型调用相关指标。


EMPLOYEE：不可见。


EMPLOYEE_REQUESTER：不可见。


十二、安全指挥台

ADMIN：可见（全局安全态势）。


ADMIN_REVIEWER：可见（只读）。


SECOPS：可见（完整功能，包括处置）。


SECOPS_RESPONDER：可见（可处置）。


EXECUTIVE：可见（只读报表）。


EXECUTIVE_COMPLIANCE：可见（只读合规报表）。


其他角色：不可见。


十三、模型谱系与漂移

ADMIN：可见，可配置漂移检测阈值。


ADMIN_REVIEWER：可见只读。


AI_BUILDER：可见（查看自己开发的模型漂移情况）。


SECOPS：可见（用于安全监控）。


EXECUTIVE：不可见（或只看摘要）。


其他角色：不可见。


十四、LSTM预测（AI风险评级）

ADMIN：可见，可触发刷新计算。


ADMIN_REVIEWER：可见只读。


SECOPS：可见。


EXECUTIVE：可见摘要。


AI_BUILDER：可见（用于评估模型风险）。


EMPLOYEE：可见（仅本人相关风险评级？通常员工不直接看这个，建议不可见）。


EMPLOYEE_REQUESTER：不可见。


其他角色：根据业务需要，可看摘要。


十五、脱敏预览

ADMIN：完全管理（配置脱敏规则、预览效果）。


ADMIN_REVIEWER：可预览，不可配置。


DATA_ADMIN：可配置数据资产脱敏规则，可预览。


DATA_ADMIN_MAINTAINER：可预览，不可配置。


SECOPS：可预览（用于验证脱敏效果），不可配置。


其他角色：不可见。


十六、主体权利管理（Subject Request）

ADMIN：完全管理（接收、处理用户的主体权利请求，如数据删除、导出）。


ADMIN_REVIEWER：可查看，可复核。


DATA_ADMIN：可处理与自己数据资产相关的主体请求。


EMPLOYEE：可发起自己的主体权利请求，可查看进度。


EMPLOYEE_REQUESTER：同样可发起。


其他角色：不可见。


十七、系统设置

ADMIN：完全管理（系统参数、邮件配置、认证配置等）。


ADMIN_REVIEWER：只读。


其他角色：不可见（除了个人资料、密码修改等个人设置）。

